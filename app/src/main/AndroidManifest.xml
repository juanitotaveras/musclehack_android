<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-sdk tools:overrideLibrary="com.anychart.anychart" />

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    
    <!-- Exact alarm permission for rest timer notifications -->
    <!-- This permission is used exclusively for workout rest timers to provide -->
    <!-- accurate notifications when rest periods are complete during training -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    
    <!-- Fallback permission for devices running Android 12+ that don't grant SCHEDULE_EXACT_ALARM -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".testimonials.TestimonialsFragmentActivity"
            android:label="@string/title_activity_testimonials_fragment"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".databackup.BackupFragmentActivity"
            android:label="@string/title_backup_activity"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".more.ContributeFragmentActivity"
            android:label="@string/contribute_title"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".about.AboutFragmentActivity"
            android:label="@string/title_activity_about"
            android:parentActivityName=".MainActivity"/>
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme"
            android:exported="true">
            <intent-filter android:label="@string/app_name">
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustPan">
        </activity> <!-- <activity -->
        <activity
            android:name=".workoutTracker.home.OptInActivity"
            android:label="OptActivity" />
        <activity
            android:name=".exerciseBank.ExerciseBankOverlayActivity"
            android:label="@string/title_activity_exercise_bank_overlay"
            android:parentActivityName=".MainActivity"/>
        <activity android:name=".trainingCycleCreation.CreatingCycleActivity" />
        <activity
            android:name=".preferences.PreferenceActivity"
            android:label="@string/title_activity_preferences"
            android:parentActivityName=".MainActivity"/>

        <service 
            android:name=".workoutTracker.training.TimerService"
            android:foregroundServiceType="shortService"
            android:exported="false" />
        <receiver android:name=".workoutTracker.training.TimerFinishedReceiver"/>
        <!-- <service -->
        <!-- android:name="gcm.play.android.samples.com.gcmquickstart.RegistrationIntentService" -->
        <!-- android:exported="false"/> -->
        <!-- set default notification icon -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="${crashlyticsEnabled}" />

        <!-- Overrides required for Android 31+ -->
        <activity
            android:name="androidx.test.core.app.InstrumentationActivityInvoker$BootstrapActivity"
            android:exported="false"
            tools:replace="android:exported"/>
        <activity
            android:name="androidx.test.core.app.InstrumentationActivityInvoker$EmptyActivity"
            android:exported="false"
            tools:replace="android:exported"/>
        <activity
            android:name="androidx.test.core.app.InstrumentationActivityInvoker$EmptyFloatingActivity"
            android:exported="false"
            tools:replace="android:exported"/>
    </application>

</manifest>